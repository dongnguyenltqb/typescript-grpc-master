syntax = "proto3";

package rpc;

import "message/user.proto";

service User{
    rpc Login(message.Credentials) returns (message.LoginResult){};
    rpc Logout(message.LogoutRequest) returns (message.LogoutResponse){};
}
